import 'package:flutter/material.dart';

class VideoCompactView extends StatelessWidget {
  const VideoCompactView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
